package com.findora.model;

/**
 * ItemCategory - item category values shared across entity, repository, and service layers.
 */
public enum ItemCategory {
    NIC,
    STUDENT_ID,
    BANK_CARD,
    WALLET,
    OTHER
}
